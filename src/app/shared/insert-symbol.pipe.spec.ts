import { InsertSymbolPipe } from './insert-symbol.pipe';

describe('InsertSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new InsertSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
