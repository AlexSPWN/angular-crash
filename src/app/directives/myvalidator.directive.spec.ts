import { MyvalidatorDirective } from './myvalidator.directive';

describe('MyvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MyvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
